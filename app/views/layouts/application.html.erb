<!DOCTYPE html>
<html>
<head>
  <title>Share3D</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <script type="importmap">
    {
      "imports": {
        "three": "https://cdn.jsdelivr.net/npm/three@0.175.0/build/three.module.min.js",
        "OBJLoader": "https://cdn.jsdelivr.net/npm/three@0.175.0/examples/jsm/loaders/OBJLoader.js",
        "OrbitControls": "https://cdn.jsdelivr.net/npm/three@0.175.0/examples/jsm/controls/OrbitControls.js"
      }
    }
  </script>

  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%= javascript_importmap_tags %>
  <%= yield :javascript %>
</head>

<body class="bg-gray-800 font-sans leading-normal tracking-normal mt-12">
  
  <%= render partial: 'layouts/components/header' %>

  <div class="flex flex-col md:flex-row">
    <%= render partial: 'layouts/components/sidebar' %>
    
    <div class="main-content flex-1 bg-gray-100 mt-12 md:mt-2 pb-24 md:pb-5">
      <div class="flex items-center justify-center">
        <%= flash_messages %>
      </div>
      <div class="flex flex-wrap p-3">
        <%= yield %>
      </div>
      <%= render partial: 'layouts/components/footer' %>
    </div>
  </div>

</body>
</html>
